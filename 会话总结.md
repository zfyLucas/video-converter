# AI 视频格式转换器 - 会话总结

## 一、项目概述

本项目是一个基于 FFmpeg 的视频格式转换工具，使用 Python + Flask + pywebview 构建，支持 12 种视频格式互转，具备完整的桌面应用体验。项目全程使用 AI（DeepSeek + Cursor）辅助开发，从需求分析到代码实现均由 AI 与开发者协作完成。

**项目名称：** AI 视频格式转换器

**项目地址：** https://github.com/zfyLucas/video-converter

**开发周期：** 3 天


## 二、技术栈

| 技术 | 用途 | 版本 |
|------|------|------|
| Python | 主语言 | 3.10+ |
| Flask | Web 后端框架 | 2.3.0+ |
| pywebview | 桌面窗口封装 | 4.0+ |
| FFmpeg | 视频转换引擎 | 6.0+ |
| OpenCV | 视频帧提取 | 4.8.0+ |
| Pillow | 图像处理 | 10.0.0+ |
| HTML + CSS + JavaScript | 前端界面 | - |


## 三、功能完成情况

### 笔试题要求（全部完成 ✅）

| 序号 | 需求 | 完成情况 | 说明 |
|------|------|----------|------|
| 1 | 加载视频信息显示 | ✅ 已完成 | 上传后显示时长、分辨率、编码、帧率、大小、格式 |
| 2 | 获取视频缩略图显示 | ✅ 已完成 | 上传时自动提取第一帧，列表中每个视频都有缩略图 |
| 3 | FFmpeg 命令行转换 | ✅ 已完成 | 使用 subprocess.Popen 调用 ffmpeg，12种格式互转 |
| 4 | 自己的优化 | ✅ 已完成 | 详见下方优化列表 |
| 5 | 会话总结 MD | ✅ 已完成 | 本文档 |

### 界面功能

| 功能 | 说明 |
|------|------|
| 文件上传 | 支持点击或拖拽上传，自动检测视频格式 |
| 格式选择 | 下拉框选择 12 种输出格式 |
| 视频转换 | 一键转换，显示进度状态 |
| 视频列表 | 所有转换记录独立展示 |
| 缩略图预览 | 上传时自动显示，列表中每个视频带缩略图 |
| 点击查看 | 点击列表中视频缩略图，右侧显示详细信息 |
| 独立下载 | 每个视频有独立的下载按钮 |
| 独立删除 | 每个视频有独立的删除按钮 |


## 四、自己的优化

### 4.1 12 种视频格式支持

支持格式：
mp4, avi, mkv, mov, flv, wmv, webm, m4v, mpg, mpeg, 3gp, 3g2

覆盖了通用格式、高清格式、网页格式、移动端格式、老设备格式等常见场景。

### 4.2 编码器自动适配

不同格式自动使用兼容的编码器，确保每种格式都能正确转换：

| 格式 | 视频编码器 | 音频编码器 | 说明 |
|------|-----------|-----------|------|
| mp4, mkv, mov, flv, 3gp, 3g2, m4v | libx264 | aac | 通用格式，兼容性最好 |
| avi | mpeg4 | mp3 | AVI 专用编码器 |
| webm | libvpx-vp9 | libopus | 网页视频专用 |
| wmv | wmv2 | wmav2 | Windows 媒体格式 |
| mpg, mpeg | mpeg2video | mp2 | DVD/VCD 格式 |

### 4.3 上传格式检测

上传文件时自动检测是否为视频格式，非视频格式报错提示用户，避免无效操作。

### 4.4 转换进度反馈

转换过程中实时显示状态：准备转换 → 正在转换 → 转换完成

### 4.5 独立下载/删除

每个视频有自己的下载和删除按钮，互不影响：
- 下载：从临时目录复制到 output 目录
- 删除：只删除临时文件和记录，不影响已下载的文件

### 4.6 点击缩略图查看详情

点击列表中视频的缩略图，右侧区域切换显示该视频的完整信息（时长、分辨率、编码、帧率、大小），与上传视频共用右侧展示区域。

### 4.7 转换临时存储

- 转换时文件保存在系统临时目录
- 点击下载时才保存到 output 目录
- 避免用户误操作导致文件混乱

### 4.8 桌面窗口封装

基于 pywebview 封装桌面窗口：
- 无需浏览器，独立运行
- 关闭窗口自动停止 Flask 服务
- 窗口尺寸自适应

### 4.9 响应式界面

页面适配不同屏幕尺寸（桌面/平板/手机），在移动设备上自动切换为单列布局。

### 4.10 一键关闭

关闭窗口时自动清理 Flask 进程和 FFmpeg 子进程，无残留进程。


## 五、项目结构

video_converter/
├── app.py                 # Flask 后端服务
├── app_webview.py         # pywebview 桌面启动器
├── video_utils.py         # 视频处理核心模块
├── templates/
│   └── index.html         # 前端界面
├── output/                # 下载输出目录
├── requirements.txt       # Python 依赖
└── 会话总结.md            # 本文档


## 六、运行方式  (在终端运行)

### 6.1 安装依赖

pip install flask flask-cors opencv-python Pillow ffmpeg-python requests pywebview

或使用 requirements.txt：

pip install -r requirements.txt

如果提示是否安装虚拟环境, 点击是

激活虚拟环境：
Windows CMD：.venv\Scripts\activate   (或者在.venv\Scripts文件里找到activate.bat, 双击运行)

Windows PowerShell：.venv\Scripts\Activate.ps1

Mac/Linux：source .venv/bin/activate

激活后，终端前面会出现 (.venv) 字样, 然后继续安装依赖

### 6.2 确保 FFmpeg 已安装

ffmpeg -version   # (添加到环境变量时终端才可以查看)

如未安装，请从 https://www.gyan.dev/ffmpeg/builds/ 下载安装，在页面里找到 ffmpeg-release-full.7z，点击下载, 解压后添加你的路径 ...\bin 到环境变量或者把 bin 文件夹里的 ffmpeg.exe 和 ffprobe.exe 放在项目文件夹里。

### 6.3 启动应用

python app_webview.py


## 七、核心代码说明

### 7.1 视频信息获取（video_utils.py）

def get_video_info(file_path):
    """使用 ffprobe 获取视频信息"""
    cmd = [
        "ffprobe",
        "-v", "quiet",
        "-print_format", "json",
        "-show_format",
        "-show_streams",
        file_path
    ]
    result = subprocess.run(cmd, capture_output=True, text=True)
    data = json.loads(result.stdout)
    # 解析时长、分辨率、编码、帧率等信息
    return info

### 7.2 编码器映射（video_utils.py）

def get_encoder(output_format):
    """根据格式返回对应的编码器"""
    codec_map = {
        'mp4': ('libx264', 'aac'),
        'webm': ('libvpx-vp9', 'libopus'),
        'wmv': ('wmv2', 'wmav2'),
        'avi': ('mpeg4', 'mp3'),
        'mpg': ('mpeg2video', 'mp2'),
        'mpeg': ('mpeg2video', 'mp2'),
    }
    return codec_map.get(output_format, ('libx264', 'aac'))

### 7.3 Flask API（app.py）

| 接口 | 方法 | 功能 |
|------|------|------|
| / | GET | 渲染主页面 |
| /api/upload | POST | 上传视频，返回信息 |
| /api/convert | POST | 执行视频转换 |
| /api/list | GET | 获取所有转换记录 |
| /api/video_info/<filename> | GET | 获取单个视频详情 |
| /api/download/<filename> | GET | 下载视频 |
| /api/delete/<filename> | DELETE | 删除视频记录 |


## 八、遇到的问题及解决方案

| 问题 | 解决方案 |
|------|----------|
| Gradio 中 HTML 按钮无法触发 Python 事件 | 改用 Flask + HTML + pywebview 架构 |
| 多个视频按钮无法区分点击 | 用 Flask API + 前端 fetch 请求实现独立操作 |
| 时长显示不一致 | 统一后端返回原始数值，前端统一格式化 |
| 不同格式编码器不兼容 | 添加 get_encoder() 函数，不同格式用不同编码器 |
| 转换时关闭窗口导致进程残留 | 使用 atexit 注册清理函数，自动终止子进程 |


## 九、总结

本项目成功实现了视频格式转换器的所有核心功能，并在此基础上做了 10 项优化。项目采用 Flask + pywebview 架构，具备完整的桌面应用体验。

项目亮点：

1. 支持 12 种视频格式互转
2. 编码器自动适配，确保兼容性
3. 独立的下载/删除功能
4. 点击缩略图查看详情
5. 桌面窗口封装，无需浏览器
6. 一键关闭，自动清理进程